{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if python3 -c \"import json,sys; payload=json.load(sys.stdin); raise SystemExit(0 if payload.get(\\\"stop_hook_active\\\") else 1)\"; then exit 0; fi; cd \"$CLAUDE_PROJECT_DIR\" && npm test >/dev/null 2>&1 || { echo \"Tests must pass before stopping\"; exit 2; }'"
          }
        ]
      }
    ]
  }
}

