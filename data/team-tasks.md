# Team Tasks

1. Add `clamp(value, min, max)` to `src/math.ts` and tests to `tests/math.test.ts`
2. Add `truncate(str, maxLen)` and `padLeft(str, len, char)` to `src/string-utils.ts` and tests to `tests/string-utils.test.ts`
3. Add `groupBy(arr, keyFn)` and `zip(a, b)` to `src/array-utils.ts` and tests to `tests/array-utils.test.ts`
